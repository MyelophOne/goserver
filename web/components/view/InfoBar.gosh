<!-- @server ./system/logic/info_bar.go#ViewInfoBarData -->
<template>
	<div
		:id="id"
		:class="infoBarClasses"
		:style="'--view-info-bar-icon-size:' + infoBarIconSize"
		:aria-label="infoBarLabel"
		role="group"
		data-view-info-bar
	>
		<slot />
	</div>
</template>

<style>
[data-view-info-bar] {
	display: flex;
	width: 100%;
	flex-wrap: wrap;
	align-items: center;
	justify-content: space-between;
	column-gap: 1rem;
	row-gap: 0.5rem;
	padding-block: 0.5rem;
	color: var(--ui-text);
}
[data-view-info-bar-item] {
	display: inline-flex;
	align-items: center;
	gap: 0.375rem;
	color: var(--ui-text);
	font-size: 0.875rem;
	font-weight: 500;
	line-height: 1;
	user-select: none;
}
[data-view-info-bar-item][data-interactive] {
	cursor: pointer;
	transition: opacity 0.15s ease;
}
[data-view-info-bar-item][data-interactive]:hover {
	opacity: 0.8;
}
[data-view-info-bar-item] [data-ui-icon],
[data-view-info-bar-action] [data-ui-icon] {
	display: block;
	width: var(--view-info-bar-icon-size);
	height: var(--view-info-bar-icon-size);
}
[data-view-info-bar-item]:active [data-ui-icon] {
	transform: scale(0.9);
}
[data-view-info-bar-action] {
	display: inline-flex;
	width: 1.75rem;
	height: 1.75rem;
	align-items: center;
	justify-content: center;
	padding: 0.25rem;
	border: 0;
	border-radius: 999px;
	color: var(--ui-text);
	background: transparent;
	cursor: pointer;
	transition: background-color 0.15s ease;
}
[data-view-info-bar-action]:first-of-type {
	margin-left: auto;
}
[data-view-info-bar-action]:hover {
	background: color-mix(in srgb, var(--ui-text), transparent 93%);
}
[data-view-info-bar-action]:focus-visible,
[data-view-info-bar-item][data-interactive]:focus-visible {
	outline: 2px solid var(--ui-focus);
	outline-offset: 2px;
}
</style>
