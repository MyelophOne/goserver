<!-- @server ./system/logic/universal_components.go#UniversalComponentData -->
<template>
	<section :id="id" :class="class" :data-align="align" data-view-creative-cta>
		<div class="cta-pattern"></div>
		<div class="cta-content">
			<div class="cta-copy">
				<h2>{{ title }}</h2>
				<p m-if="description">{{ description }}</p>
			</div>
			<div class="cta-actions">
				<a m-if="primaryText" :href="primaryLink" class="cta-primary">{{
					primaryText
				}}</a
				><a
					m-if="secondaryText"
					:href="secondaryLink"
					class="cta-secondary"
					>{{ secondaryText }}</a
				><slot />
			</div>
		</div>
	</section>
</template>
<style>
[data-view-creative-cta] {
	position: relative;
	isolation: isolate;
	width: 100%;
	overflow: hidden;
	border-radius: 1rem;
	background: var(--ui-bg);
	box-shadow: 0 1.25rem 2.5rem #0002;
}
[data-view-creative-cta] .cta-pattern {
	position: absolute;
	inset: 0;
	z-index: -1;
	opacity: 0.05;
	background-image: radial-gradient(var(--ui-text) 1px, transparent 1px);
	background-size: 24px 24px;
}
[data-view-creative-cta] .cta-content {
	display: flex;
	flex-direction: column;
	padding: 3rem 1.5rem;
	text-align: left;
}
[data-view-creative-cta][data-align="center"] .cta-content {
	text-align: center;
	align-items: center;
}
[data-view-creative-cta][data-align="right"] .cta-content {
	text-align: right;
	align-items: end;
}
.cta-copy {
	max-width: 56rem;
}
.cta-copy h2 {
	margin: 0;
	font-size: clamp(1.875rem, 4vw, 3rem);
	font-weight: 700;
	line-height: 1.2;
	letter-spacing: -0.025em;
}
.cta-copy p {
	max-width: 42rem;
	margin: 1rem 0 0;
	font-size: clamp(1rem, 2vw, 1.125rem);
	line-height: 1.625;
	opacity: 0.9;
}
.cta-actions {
	display: flex;
	flex-direction: column;
	gap: 1rem;
	width: 100%;
	margin-top: 2rem;
}
.cta-primary,
.cta-secondary {
	display: inline-flex;
	align-items: center;
	justify-content: center;
	padding: 0.875rem 1.5rem;
	border-radius: 0.5rem;
	font-size: 0.875rem;
	font-weight: 700;
	letter-spacing: 0.05em;
	text-decoration: none;
	text-transform: uppercase;
	transition: opacity 0.2s;
}
.cta-primary {
	color: var(--ui-bg);
	background: var(--ui-text);
}
.cta-secondary {
	border: 2px solid color-mix(in srgb, var(--ui-text), transparent 80%);
	color: var(--ui-text);
}
.cta-primary:hover,
.cta-secondary:hover {
	opacity: 0.9;
}
@media (min-width: 640px) {
	[data-view-creative-cta] .cta-content {
		padding: 5rem 3rem;
	}
	.cta-actions {
		width: auto;
		flex-direction: row;
	}
}
</style>
