<template>
	<details :id="id" :class="class" :open="defaultOpen" data-ui-accordion-item>
		<summary data-ui-accordion-header>
			<span data-ui-accordion-label>{{ label }}</span>
			<span aria-hidden="true" data-ui-accordion-icon>
				<svg
					viewBox="0 0 24 24"
					fill="none"
					stroke="currentColor"
					stroke-width="2"
					stroke-linecap="round"
					stroke-linejoin="round"
				>
					<polyline points="6 9 12 15 18 9"></polyline>
				</svg>
			</span>
		</summary>
		<div data-ui-accordion-content><slot /></div>
	</details>
</template>

<style scoped>
[data-ui-accordion-item] {
	overflow: hidden;
	border: 1px solid var(--ui-border, #d4d4d8);
	border-radius: 0.5rem;
	background: var(--ui-surface, var(--ui-bg));
	color: var(--ui-text);
}
[data-ui-accordion-header] {
	display: flex;
	align-items: center;
	justify-content: space-between;
	gap: 1rem;
	padding: 1rem;
	font-size: 1rem;
	font-weight: 600;
	line-height: 1.5;
	cursor: pointer;
	list-style: none;
	transition:
		background-color 0.2s ease,
		color 0.2s ease;
}
[data-ui-accordion-header]::-webkit-details-marker {
	display: none;
}
[data-ui-accordion-header]:hover,
[data-ui-accordion-item][open] [data-ui-accordion-header] {
	background: color-mix(in srgb, var(--ui-text), transparent 94%);
	color: var(--ui-focus, #2563eb);
}
[data-ui-accordion-header]:focus-visible {
	outline: 2px solid var(--ui-focus, #2563eb);
	outline-offset: -2px;
}
[data-ui-accordion-icon] {
	display: inline-flex;
	width: 1.25rem;
	height: 1.25rem;
	flex: none;
	color: color-mix(in srgb, var(--ui-text), transparent 45%);
	transition: transform 0.3s ease;
}
[data-ui-accordion-icon] svg {
	width: 100%;
	height: 100%;
}
[data-ui-accordion-item][open] [data-ui-accordion-icon] {
	transform: rotate(180deg);
}
[data-ui-accordion-content] {
	padding: 0 1rem 1rem;
	border-top: 1px solid
		color-mix(in srgb, var(--ui-border, #d4d4d8), transparent 45%);
	color: color-mix(in srgb, var(--ui-text), transparent 25%);
	line-height: 1.625;
}
[data-ui-accordion-content] > :first-child {
	margin-top: 1rem;
}
[data-ui-accordion-content] > :last-child {
	margin-bottom: 0;
}
</style>
